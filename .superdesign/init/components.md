# Shared UI components

This server-rendered Jinja application has no separate shared component directory. Reusable UI markup is deliberately kept in the base layout; page-specific controls are in their corresponding templates.

No standalone primitive component source files exist.

