# Spike 结果记录（P0–P8）

环境：OS=Windows 11  Codex CLI 版本=0.144.6  桌面端版本=未知  uv 版本=uvx 0.11.9 (7829a03b6 2026-05-05 x86_64-pc-windows-msvc)  日期=2026-07-18

| # | 验证项摘要 | 结果 | 证据（命令+输出摘要） | 采用路径（主/备选/未验证） | 需更新的 spec 章节 |
|---|---|---|---|---|---|
| P0 | 仅桌面端环境的 marketplace 发现 | 部分成立 | 已证实：①`codex plugin marketplace add` 的本质是向 `~/.codex/config.toml` 写入 `[marketplaces.<name>]`（source_type/source/last_updated）——bootstrap 无 CLI 时可用 TOML 解析安全追加；②桌面端读取该共享配置生效（P3 用户实测）；③安装状态 = `~/.codex/plugins/cache/<marketplace>/<plugin>/<version>/` 文件 + `[plugins."<name>@<marketplace>"] enabled=true`（`plugin remove` 会同时清缓存与配置节，已观察）。未实测：手工填充缓存+写配置节的"无 CLI 安装"（用户中断该实验）；**桌面端插件目录视图不含自定义 marketplace 插件（用户确认：有目录但看不到 vibe-submit-spike）→ 无 CLI 时无法经桌面 UI 安装**。 | bootstrap 策略：有 CLI 用 CLI；无 CLI 时写 `[marketplaces.*]` 注册节并**引导补装 Codex CLI**（一条命令），手工填充缓存+写配置节作为文档化兜底（未实测）。 | §3.2, §11 |
| P1 | 插件 .mcp.json 在 CLI 启动 MCP；env 字段 | 成立 | `codex plugin marketplace add ./spikes/marketplace` 注册成功；`codex plugin add vibe-submit-spike@vibe-course-spike` 安装成功（缓存于 `~/.codex/plugins/cache/vibe-course-spike/vibe-submit-spike/0.1.0/`）。`codex exec --dangerously-bypass-approvals-and-sandbox "调用…ping…"` → `mcp: vibe-submit-spike/ping (completed)` → `pong v0.1.0 python=3.13.13`。Skill 随插件可用（agent 读取插件缓存中的 SKILL.md 并按其引导行动）。env 字段：`.mcp.json` 加 `"env":{"UV_INDEX_URL":…}`，remove+add 重装后 `env_check` → `UV_INDEX_URL=SET`（**文档未列但 0.144.6 实测支持**）。注意：Windows 沙箱助手缺失（`codex-windows-sandbox-setup.exe program not found`）导致带沙箱运行时 shell 命令失败、MCP 调用被取消，须 `--dangerously-bypass-approvals-and-sandbox` 绕过（详情交 P2 跟进）。 | 主路径：插件 `.mcp.json`（`uvx --from vibe-submit==X.Y.Z vibe-submit-mcp`）直启 MCP；镜像可经 `env` 字段注入。 | §3.1, §11 |
| P2 | Windows 端到端；GUI PATH 陈旧 | 部分成立 | 本机当日新装 codex 0.144.6 + 既有 uv 0.11.9，插件全流程通过（见 P1）。GUI PATH：ChatGPT 桌面端（GUI 启动）Codex 会话调用 ping → `pong v0.1.0 python=3.13.13`（用户实测），**PATH 陈旧未复现**（uv 经官方安装器写入用户 PATH，与 codex 安装器同机制）。**重大附带发现**：`codex-windows-sandbox-setup.exe` 不在安装目录（仅有 codex.exe / codex-code-mode-host.exe），read-only 与 workspace-write 沙箱下 shell 命令全部失败（`orchestrator_helper_launch_failed`），须 `--dangerously-bypass-approvals-and-sandbox` 绕过；**MCP 调用不受影响**（独立进程）——对插件非阻塞，但学生正常 Codex shell 使用会受影响，属平台问题（Windows 支持官方标注 experimental），需进课程 FAQ。干净 Windows 用户/VM 未实测。 | 主路径可行；`doctor` 保留绝对路径兜底；沙箱问题写课程 FAQ 并跟踪官方修复。 | §3.2, §7, §11 |
| P3 | 桌面端读取 CLI 注册的 marketplace | 成立 | 桌面端设置→插件→MCP 页"来自插件"列表显示 `vibe-submit-spike`（用户截图实证）；桌面端 Codex 会话调用 ping → `pong v0.1.0`（用户实测）。证实桌面端与 CLI 共享 `~/.codex` 配置体系（config.toml 中 `[marketplaces.vibe-course-spike]` 与 `[plugins."vibe-submit-spike@vibe-course-spike"] enabled=true` 即注册/启用状态）。 | 主路径：桌面端用户与 CLI 用户同等待遇；bootstrap 也可在无 CLI 时直接维护 config.toml（关联 P0）。 | §3.2, §11 |
| P4 | IDE 扩展读 config.toml [mcp_servers] | 成立 | `~/.codex/config.toml` 追加 `[mcp_servers.vibe-submit-spike]`（`uvx --from <wheel> vibe-submit-mcp`）后：CLI 层 `codex mcp list` 显示 `vibe-submit-spike enabled`；VS Code Codex 扩展（openai.chatgpt-26.715.31925）会话中调用 `ping` → `pong v0.1.0 python=3.13.13`（用户实测）。实验后移除该配置节，`codex mcp list` 不再显示——uninstall 语义成立。 | 主路径成立：IDE 扩展为兼容路径，bootstrap 直接写 `config.toml` 注册即可。 | §3.2, §11 |
| P5 | git-subdir 布局；Gitee 兼容 | 部分成立 | GitHub 全流程实测通过：`codex plugin marketplace add JasonLuo365/vibe-course-marketplace`（shorthand 与完整 URL 均可）→ 快照至 `~/.codex/.tmp/marketplaces/` → `plugin add` 安装 → MCP 注册成功。**布局要求**：marketplace.json 必须在**仓库根** `.agents/plugins/marketplace.json`；子目录布局（`course/.agents/...`）两种 sparse 写法均报 "marketplace root does not contain a supported manifest"——**marketplace add 不支持子目录**（`git-subdir` source 类型是 marketplace.json 内部插件条目的概念，非 add 命令能力）。`--sparse` 仅为裁剪 checkout 的优化：只写 `--sparse .agents/plugins` 会缺 `plugins/` 目录导致安装失败（"plugin source path is not a directory"），**必须双路径** `--sparse .agents/plugins --sparse plugins`（根布局可省略 sparse）。Gitee：未验证（无账号）。 | 主路径：GitHub（或任意 git 托管）根布局 marketplace；Gitee 上线前补验。 | §3.1, §9, §11 |
| P6 | 仓库级 marketplace 自动发现（可选） | 不成立（CLI 层） | 在含 `.agents/plugins/marketplace.json` 的 git 仓库内执行 `codex plugin marketplace list` 与 `codex plugin list`：均只显示全局已注册源，**未出现仓库级源**；无信任提示（非交互命令层）。TUI `/plugins` 浏览器内是否有信任式发现未验证（需人工），但即使存在也依赖学生先在仓库内打开 TUI。 | 备选：放弃自动发现增强，只保留显式 `codex plugin marketplace add`（由 bootstrap 完成）。 | §11 |
| P7 | uvx --from 钉版与升级行为 | 成立 | 本地 wheel（PowerShell）：<br>① `uvx --from ./dist/vibe_submit-0.1.0-py3-none-any.whl vibe-submit --version` → `vibe-submit 0.1.0`；<br>② 改 `pyproject.toml` version 为 `0.2.0` 后 `uv build`，`uvx --from ./dist/vibe_submit-0.2.0-py3-none-any.whl vibe-submit --version` → `vibe-submit 0.2.0`；<br>③ 再次调用 0.1.0 wheel 仍返回 `vibe-submit 0.1.0`。PyPI 钉版：<br>① `uvx --from "httpx==0.28.0" python -c "import httpx;print(httpx.__version__)"` → `0.28.0`；<br>② `==0.27.0` 在主 PyPI 因解析到不兼容 h11 而 ImportError；<br>③ 子进程注入 Tsinghua 镜像后重试：`$env:UV_INDEX_URL='https://pypi.tuna.tsinghua.edu.cn/simple'; uvx --from "httpx==0.27.0" python -c "import httpx;print(httpx.__version__)"` → `0.27.0`。 | 主路径：`uvx --from vibe-submit==X.Y.Z vibe-submit-mcp`（本地 wheel/PyPI 均可按版本隔离）；旧版与新版本可并存调用，互不影响。 | §3.1, §3.3, §11 |
| P8 | 镜像局部化方案 | 成立 | 基线：`Test-Path $env:APPDATA\uv\uv.toml` → `False`；`pip config list` → 无输出（无全局 index-url）。<br>子进程 env：`$env:UV_INDEX_URL='https://pypi.tuna.tsinghua.edu.cn/simple'; uvx --from ./spikes/hello-pkg/dist/vibe_submit-0.1.0-py3-none-any.whl vibe-submit --version` → `vibe-submit 0.1.0`；新 shell `Test-Path env:UV_INDEX_URL` → `False`。<br>cwd-local uv.toml：`spikes/uv.toml` 含 `[[index]] url = "https://pypi.tuna.tsinghua.edu.cn/simple" default = true`；在 `spikes/` 下 `uvx --from ./hello-pkg/dist/vibe_submit-0.1.0-py3-none-any.whl vibe-submit --version` → `vibe-submit 0.1.0`；复查 `%APPDATA%\uv\uv.toml` 仍不存在。 | 优先子进程环境变量（作用域最小）；cwd-local `uv.toml` 为备选。 | §3.2, §7, §11 |
